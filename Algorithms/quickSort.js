// async function quickSortArray() {
//   let index = 0;
//   let chunks = document.getElementsByClassName("chunk");

//   if (elements.length > 1) {
//     index = await partition(elements, left, right);

//     if (left < index - 1) {
//       await quickSortArray(items, index, left - 1);
//     }

//     if (index < right) {
//       await quickSortArray(items, index, right);
//     }
//   }

//   for (let i = 0; i < elements.length; i++) {
//     chunks[i].style.backgroundColor = "yellow";
//   }
//   return elements;
// }
